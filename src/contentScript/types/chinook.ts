const pagerSelector = 'div.pager-text'

const pagerInputSelector = 'div.airdrop > input'

const pagerButtonsSelectorAll = 'div.airdrop-keylock__grid-num' // check by style.opacity = 1 if not clicked

const pagerButtonValueSelector = 'div.wrap-num' // .textContent

export {}